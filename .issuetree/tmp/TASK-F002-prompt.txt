You are working on task TASK-1 in issue ISSUE-F002.

Task: Implement Database Schema with Prisma ORM

Description:
Implement the database schema for BridgeAI platform using Prisma ORM.

Key requirements:
1. Set up Prisma ORM with PostgreSQL
2. Design and implement core entity models:
   - User (authentication, profile, metadata)
   - Agent (configuration, persona, settings)
   - Scene (VisionShare, AgentDate, AgentJob, AgentAd configurations)
   - Chat/Message (communication history)
   - Credit/Transaction (scoring system)
   - Matching (preferences, filters, results)
3. Define relationships between entities
4. Create migration files
5. Generate Prisma client
6. Add seed data for development

Work in the worktree: D:/projects/visionshare/.issuetree/issues/ISSUE-F002

Acceptance Criteria:
- Prisma schema is complete with all core models
- Migrations can be applied successfully
- Database connection is configured
- Basic CRUD operations are testable

Instructions:
1. First, update the task status to in_progress in the YAML file:
   .issuetree/issues/ISSUE-F002/tasks/TASK-1~prisma-database-schema.yaml

2. Read the issue YAML to understand full requirements:
   .issuetree/issues/ISSUE-F002/ISSUE-F002.yaml

3. Create a git worktree for this issue at:
   D:/projects/visionshare/.issuetree/issues/ISSUE-F002

4. Implement the database schema with Prisma ORM in the worktree

5. On completion, update the task YAML:
   - Set status to "passed"
   - Fill in result, session_end, execution_time_minutes
   - List files_touched

6. On failure, set status to "failed" and describe the error in notes
