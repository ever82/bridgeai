---
id: "ISSUE-F004"
issue_number: 104
title: "Expo移动端项目初始化"
slug: "expo-mobile"
status: "pending"
description: |
  初始化 Expo React Native 移动端项目，配置开发环境、
  导航结构、状态管理、网络请求等基础设施，支持 iOS/Android 双平台。
prerequisites:
  - "ISSUE-F001"
created_at: "2026-04-08"
logic: sufficient_necessary
criteria:
  - id: "ISSUE-F004~c1"
    name: "Expo 项目初始化"
    description: |
      配置 Expo 项目:
      - 使用 create-expo-app 初始化项目
      - 配置 Expo SDK 和依赖
      - 设置 TypeScript 支持
      - 配置 metro bundler
    status: "pending"
    coverage:
      unit: not_needed
      integration: not_needed
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 apps/mobile/ 目录是否有 Expo 项目结构，验证 app.json 和 package.json 配置正确"
        context_files: ["apps/mobile/app.json", "apps/mobile/package.json"]
        

  - id: "ISSUE-F004~c2"
    name: "导航结构配置"
    description: |
      设置导航系统:
      - 配置 React Navigation
      - 设置底部 Tab 导航
      - 配置 Stack 导航器
      - 实现深层链接配置
    status: "pending"
    coverage:
      unit: not_needed
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查导航配置，验证使用 Expo Router 或 React Navigation，确认有 Tab 和 Stack 导航结构"
        context_files: ["apps/mobile/src/navigation/", "apps/mobile/app/"]
        

  - id: "ISSUE-F004~c3"
    name: "全局状态管理"
    description: |
      配置状态管理:
      - 集成 Zustand/Redux Toolkit
      - 设置用户状态存储
      - 配置持久化存储(AsyncStorage)
      - 状态类型定义
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查状态管理配置，验证有 store 定义、持久化配置和类型定义"
        context_files: ["apps/mobile/src/store/"]
        

  - id: "ISSUE-F004~c4"
    name: "网络请求和 API 客户端"
    description: |
      配置 API 通信:
      - 集成 Axios/Fetch
      - 配置请求/响应拦截器
      - 设置 API 基础 URL
      - 请求错误处理和重试机制
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 API 客户端配置，验证有 baseURL 配置、拦截器处理和错误处理"
        context_files: ["apps/mobile/src/api/client.ts"]
        

  - id: "ISSUE-F004~c5"
    name: "UI 组件库和主题"
    description: |
      设置 UI 基础设施:
      - 配置 NativeWind/Tamagui/原生样式
      - 设置主题和颜色系统
      - 创建基础组件库(Button, Input, Card等)
      - 响应式布局适配
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 UI 组件和主题配置，验证有设计系统、主题定义和基础组件实现"
        context_files: ["apps/mobile/src/components/", "apps/mobile/src/theme/"]
        

  - id: "ISSUE-F004~c6"
    name: "权限和原生模块配置"
    description: |
      配置原生权限:
      - 相机权限配置
      - 位置权限配置
      - 通知权限配置
      - 存储权限配置
    status: "pending"
    coverage:
      unit: not_needed
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 app.json 中的权限配置，验证有 camera, location, notifications 等权限声明"
        context_files: ["apps/mobile/app.json"]
        
