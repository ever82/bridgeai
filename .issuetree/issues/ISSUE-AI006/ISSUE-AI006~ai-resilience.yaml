---
id: "ISSUE-AI006"
issue_number: 506
title: "AI服务降级与容错策略"
slug: "ai-resilience"
status: "pending"
description: |
  实现AI服务降级与容错策略，在LLM服务不可用或响应过慢时
  提供优雅的降级方案，包括缓存回退、简化模型、预设回复、
  异步队列等机制，确保系统核心功能可用性。
prerequisites:
  - "ISSUE-AI001"
  - "ISSUE-AI004"
created_at: "2026-04-08"
logic: sufficient_necessary
criteria:
  - id: "ISSUE-AI006~c1"
    name: "多级降级策略"
    description: |
      降级层级:
      - L1: 主模型降级到备用模型
      - L2: 高质量降级到低成本模型
      - L3: AI生成降级到模板回复
      - L4: 实时降级到异步队列
      - L5: 服务完全不可用提示
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查降级策略，验证有DegradationStrategy类或配置，支持5个降级级别，每个级别有对应的fallback实现和触发条件"
        context_files: ["apps/server/src/services/ai/resilience/degradationStrategy.ts"]
        

  - id: "ISSUE-AI006~c2"
    name: "智能降级决策器"
    description: |
      决策系统:
      - 服务健康状态监控
      - 降级级别自动判断
      - 用户影响评估
      - 渐进式降级/恢复
      - 降级原因记录
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查降级决策器，验证有DegradationController类，监控error_rate、latency_percentile，自动选择degradation_level，记录decision_reason"
        context_files: ["apps/server/src/services/ai/resilience/degradationController.ts"]
        

  - id: "ISSUE-AI006~c3"
    name: "模板回复系统"
    description: |
      模板机制:
      - 场景模板库
      - 变量替换支持
      - 模板匹配算法
      - 个性化模板选择
      - 模板动态更新
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查模板系统，验证有TemplateResponseService类，包含templates配置，支持变量插值{{variable}}，根据scene和intent匹配模板"
        context_files: ["apps/server/src/services/ai/resilience/templateResponseService.ts"]
        

  - id: "ISSUE-AI006~c4"
    name: "响应缓存回退"
    description: |
      缓存机制:
      - 相似请求缓存
      - 缓存命中策略
      - 缓存过期管理
      - 降级时优先使用
      - 缓存预热
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查缓存回退，验证有AIResponseCacheService类，使用similarity_hash索引缓存，支持TTL过期，degradation时返回stale cache"
        context_files: ["apps/server/src/services/ai/resilience/responseCacheService.ts"]
        

  - id: "ISSUE-AI006~c5"
    name: "异步处理队列"
    description: |
      队列系统:
      - 延迟任务队列
      - 优先级管理
      - 失败重试机制
      - 队列状态查询
      - 结果回调通知
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查异步队列，验证有AIAsyncQueueService类，使用Redis/Bull队列，支持priority、retry_count，提供job status查询API"
        context_files: ["apps/server/src/services/ai/resilience/asyncQueueService.ts"]
        

  - id: "ISSUE-AI006~c6"
    name: "降级监控与告警"
    description: |
      监控告警:
      - 降级事件统计
      - 降级持续时间
      - 用户影响评估
      - 自动告警触发
      - 恢复通知
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查降级监控，验证有ResilienceMetricsService类，暴露降级metrics：ai_degradation_events_total、degradation_duration_seconds，集成告警"
        context_files: ["apps/server/src/services/ai/resilience/metricsService.ts"]
        
