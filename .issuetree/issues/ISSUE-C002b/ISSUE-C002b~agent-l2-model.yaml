---
id: "ISSUE-C002b"
issue_number: 303
title: "L2结构化信息模型"
slug: "agent-l2-model"
status: "accepted"
description: |
  实现 Agent L2 结构化信息模型，支持用户通过选择/填写
  预设选项来描述需求/供给，用于过滤和匹配。
prerequisites:
  - "ISSUE-C002a"
created_at: "2026-04-08"
logic: sufficient_necessary
criteria:
  - id: "ISSUE-C002b~c1"
    name: "L2 Schema 系统"
    description: |
      结构化信息定义:
      - 场景特定的 L2 Schema
      - 字段类型: enum, range, boolean, multi_select
      - 条件字段(dependent fields)
      - Schema 版本管理
    status: "accepted"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 L2 Schema 定义，验证有各场景的 L2 Schema JSON 文件，包含字段类型和选项"
        context_files: ["packages/shared/src/schemas/l2/"]
        

  - id: "ISSUE-C002b~c2"
    name: "L2 动态表单生成"
    description: |
      表单渲染:
      - 根据 Schema 动态生成表单
      - 条件字段显示逻辑
      - 字段联动
      - 移动端适配
    status: "accepted"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查动态表单实现，验证有 DynamicForm 组件可根据 Schema 渲染不同类型的字段"
        context_files: ["apps/mobile/src/components/DynamicForm/"]
        

  - id: "ISSUE-C002b~c3"
    name: "L2 数据存储和查询"
    description: |
      L2 存储:
      - JSON 字段存储
      - GIN 索引支持复杂查询
      - 数据验证 middleware
      - 历史版本存储
    status: "accepted"
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 L2 存储，验证 AgentProfile 有 l2Data JSON 字段和 GIN 索引"
        context_files: ["prisma/schema.prisma"]
        

  - id: "ISSUE-C002b~c4"
    name: "L2 编辑和预览"
    description: |
      L2 配置界面:
      - 分步骤表单
      - 实时预览
      - 自动保存草稿
      - 完成度展示
    status: "accepted"
    coverage:
      unit: not_needed
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 L2 编辑界面，验证有分步骤表单和预览功能"
        context_files: ["apps/mobile/src/screens/AgentProfile/L2ProfileScreen.tsx"]
        

  - id: "ISSUE-C002b~c5"
    name: "L2 验证和约束"
    description: |
      数据验证:
      - Schema 验证
      - 必填字段检查
      - 选项值白名单验证
      - 验证错误提示
    status: "accepted"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 L2 验证，验证有 validateL2Data 函数根据 Schema 验证数据"
        context_files: ["apps/server/src/utils/l2Validation.ts"]
        
