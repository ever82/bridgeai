---
id: 'ISSUE-F002'
issue_number: 102
title: '数据库设计与Prisma ORM搭建'
slug: 'database-prisma'
status: passed
checked_at: '2026-04-09'
check:
  status: passed
  functional_verification:
    all_criteria_passed: true
  test_execution:
    all_tests_passed: true
  notes: 'Prisma schema with User, Agent, Demand models and migrations verified'
description: |
  设计 BridgeAI 平台的数据库 schema，配置 Prisma ORM，
  包括用户、Agent、消息、匹配、信用等核心实体的数据模型定义，
  以及数据库迁移和种子数据管理。
prerequisites:
  - 'ISSUE-F001'
created_at: '2026-04-08'
logic: sufficient_necessary
criteria:
  - id: 'ISSUE-F002~c1'
    name: 'Prisma 初始化和配置'
    description: |
      配置 Prisma ORM:
      - 安装 @prisma/client 和 prisma CLI
      - 配置 schema.prisma 基础设置(数据源、生成器)
      - 设置数据库连接字符串管理
      - 配置 Prisma Client 单例模式
    status: 'pending'
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: '检查 schema.prisma 是否存在且包含 datasource db 和 generator client 配置，确认 prisma 目录结构和客户端生成配置正确'
        context_files: ['prisma/schema.prisma', 'packages/database/src/client.ts']

  - id: 'ISSUE-F002~c2'
    name: '核心数据模型定义'
    description: |
      定义核心实体模型:
      - User (用户): id, email, phone, password, profile, createdAt, updatedAt
      - Agent (智能体): id, userId, name, type, config, status, creditScore
      - AgentProfile (Agent资料): L1基础信息, L2结构化信息, L3自然语言描述
      - Scene (场景): VisionShare, AgentDate, AgentJob, AgentAd
      - 关系定义和索引优化
    status: 'pending'
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: '检查 schema.prisma 中是否定义了 User, Agent, AgentProfile, Scene 等模型，验证字段类型、关系和索引配置'
        context_files: ['prisma/schema.prisma']

  - id: 'ISSUE-F002~c3'
    name: '消息和通信模型'
    description: |
      定义通信相关模型:
      - ChatRoom (聊天室): id, type, sceneId, participants, metadata
      - Message (消息): id, roomId, senderId, content, type, status, createdAt
      - Connection (连接): socket连接管理、在线状态
      - 消息已读状态追踪
    status: 'pending'
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: '检查 schema.prisma 中是否定义了 ChatRoom, Message, Connection 等通信相关模型，验证消息状态枚举和关系'
        context_files: ['prisma/schema.prisma']

  - id: 'ISSUE-F002~c4'
    name: '匹配和信用模型'
    description: |
      定义匹配和信用相关模型:
      - Match (匹配记录): matcherId, matcheeId, score, status, scene
      - CreditRecord (信用记录): userId, score, reason, delta
      - Transaction (积分交易): userId, amount, type, description
      - Rating (评分): fromUserId, toUserId, score, comment
    status: 'pending'
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: '检查 schema.prisma 中是否定义了 Match, CreditRecord, Transaction, Rating 等模型，验证字段和关系'
        context_files: ['prisma/schema.prisma']

  - id: 'ISSUE-F002~c5'
    name: '数据库迁移系统'
    description: |
      建立数据库迁移流程:
      - 创建初始迁移文件
      - 配置迁移脚本命令
      - 设置迁移回滚机制
      - 开发环境和生产环境迁移策略
    status: 'pending'
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: '检查 prisma/migrations/ 目录是否存在迁移文件，验证 package.json 中有 prisma migrate 相关脚本命令'
        context_files: ['prisma/migrations/migration_lock.toml', 'package.json']

  - id: 'ISSUE-F002~c6'
    name: '种子数据和数据库工具'
    description: |
      创建种子数据和工具:
      - seed.ts 种子数据脚本
      - 开发环境测试数据生成
      - Prisma Studio 配置
      - 数据库查询工具和辅助函数
    status: 'pending'
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: '检查 prisma/seed.ts 是否存在，验证种子数据包含测试用户、Agent和场景配置，确认有 db:seed 脚本'
        context_files: ['prisma/seed.ts', 'package.json']

  - id: 'ISSUE-F002~c7'
    name: '数据库索引和性能优化'
    description: |
      优化数据库性能:
      - 为常用查询字段添加索引
      - 配置复合索引
      - 设置适当的外键约束
      - 文档记录查询优化策略
    status: 'pending'
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: '检查 schema.prisma 中关键模型是否有 @@index 定义，验证常用查询字段(如 userId, agentId, createdAt)有索引'
        context_files: ['prisma/schema.prisma']
