---
id: "ISSUE-F003"
issue_number: 103
title: "Express后端基础框架"
slug: "express-backend"
status: "pending"
description: |
  搭建 Express 后端服务基础框架，包括路由结构、中间件配置、
  错误处理、请求/响应标准化、API 版本控制等核心基础设施。
prerequisites:
  - "ISSUE-F001"
created_at: "2026-04-08"
logic: sufficient_necessary
criteria:
  - id: "ISSUE-F003~c1"
    name: "Express 应用初始化"
    description: |
      配置 Express 应用:
      - 安装 Express 和相关中间件
      - 配置 body-parser, cors, helmet
      - 设置应用入口文件 (app.ts, server.ts)
      - 配置开发热重载
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 apps/server/src/app.ts 和 server.ts 是否存在，验证 Express 应用配置包含 cors, helmet, body-parser 中间件"
        context_files: ["apps/server/src/app.ts", "apps/server/src/server.ts", "apps/server/package.json"]
        

  - id: "ISSUE-F003~c2"
    name: "路由结构设计和实现"
    description: |
      建立模块化路由结构:
      - routes/ 目录结构
      - API 版本控制 (v1, v2)
      - 路由模块化组织 (auth, user, agent, chat, match等)
      - 路由前缀和命名规范
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 apps/server/src/routes/ 目录结构，验证有 v1 或版本控制目录，确认路由按功能模块组织"
        context_files: ["apps/server/src/routes/index.ts", "apps/server/src/routes/v1/"]
        

  - id: "ISSUE-F003~c3"
    name: "全局中间件配置"
    description: |
      配置全局中间件:
      - 请求日志中间件
      - 错误处理中间件
      - 请求超时处理
      - 请求 ID 注入
      - 压缩和静态文件服务
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查中间件配置文件，验证有 errorHandler, requestLogger, timeout 等全局中间件实现"
        context_files: ["apps/server/src/middleware/"]
        

  - id: "ISSUE-F003~c4"
    name: "统一响应格式和错误处理"
    description: |
      建立标准化响应:
      - ApiResponse 统一响应类
      - 错误类和错误码定义
      - HTTP 状态码映射
      - 异常捕获和转换
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查是否有统一的响应和错误处理类，验证 ApiResponse 包含 success, data, message, errorCode 字段"
        context_files: ["apps/server/src/utils/response.ts", "apps/server/src/errors/"]
        

  - id: "ISSUE-F003~c5"
    name: "环境配置和启动脚本"
    description: |
      配置运行环境:
      - 不同环境的配置加载
      - 开发服务器启动脚本
      - 生产环境 PM2/Docker 配置
      - 端口和主机配置
    status: "pending"
    coverage:
      unit: not_needed
      integration: required
      e2e: not_needed
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 package.json 脚本，验证有 dev, build, start, 和 docker 相关命令，确认端口配置正确"
        context_files: ["apps/server/package.json", "apps/server/Dockerfile"]
        

  - id: "ISSUE-F003~c6"
    name: "健康检查和监控端点"
    description: |
      实现系统健康检查:
      - /health 健康检查端点
      - /ready 就绪探针
      - /metrics 指标端点(基础)
      - 数据库连接状态检查
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查是否有 /health 和 /ready 端点实现，验证返回包含 status, timestamp, checks 等字段"
        context_files: ["apps/server/src/routes/health.ts"]
        
