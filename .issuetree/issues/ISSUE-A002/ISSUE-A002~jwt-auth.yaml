---
id: "ISSUE-A002"
issue_number: 202
title: "JWT认证与API安全"
slug: "jwt-auth"
status: "pending"
description: |
  实现基于 JWT 的 API 认证机制，包括 Token 生成与验证、
  刷新令牌机制、Token 黑名单、API 权限控制等安全功能。
prerequisites:
  - "ISSUE-A001"
created_at: "2026-04-08"
logic: sufficient_necessary
criteria:
  - id: "ISSUE-A002~c1"
    name: "JWT Token 生成和验证"
    description: |
      JWT 基础功能:
      - Access Token 生成
      - Token 签名和验证
      - Token 过期时间配置
      - JWT Payload 结构设计
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 JWT 实现，验证使用 jsonwebtoken 库，确认 Token 包含 userId, role, iat, exp 字段"
        context_files: ["apps/server/src/utils/jwt.ts", "apps/server/src/middleware/auth.ts"]
        model: "claude-sonnet-4-6"

  - id: "ISSUE-A002~c2"
    name: "Refresh Token 机制"
    description: |
      刷新令牌:
      - Refresh Token 生成
      - Token 刷新接口
      - Refresh Token 存储
      - 刷新限制和轮换
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 Refresh Token 实现，验证有 /api/v1/auth/refresh 端点，确认 Refresh Token 存储在数据库/Redis"
        context_files: ["apps/server/src/routes/auth.ts", "apps/server/src/services/tokenService.ts"]
        model: "claude-sonnet-4-6"

  - id: "ISSUE-A002~c3"
    name: "认证中间件"
    description: |
      API 认证中间件:
      - Token 提取(Header/Cookie)
      - Token 验证
      - 用户信息注入
      - 401/403 错误处理
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查认证中间件，验证 authenticate 中间件正确验证 JWT，并注入 req.user"
        context_files: ["apps/server/src/middleware/auth.ts"]
        model: "claude-sonnet-4-6"

  - id: "ISSUE-A002~c4"
    name: "Token 黑名单和撤销"
    description: |
      Token 管理:
      - 登出时 Token 撤销
      - Token 黑名单(Redis)
      - 用户密码修改时撤销所有 Token
      - 黑名单定期清理
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查 Token 黑名单实现，验证登出时将 Token 加入黑名单，确认使用 Redis 存储"
        context_files: ["apps/server/src/services/tokenService.ts", "apps/server/src/config/redis.ts"]
        model: "claude-sonnet-4-6"

  - id: "ISSUE-A002~c5"
    name: "移动端 Token 管理"
    description: |
      移动端认证:
      - Token 本地存储(安全存储)
      - 请求自动附加 Token
      - Token 过期自动刷新
      - 刷新失败重新登录
    status: "pending"
    coverage:
      unit: required
      integration: required
      e2e: required
      ai: sufficient
      manual: not_needed
    verification:
      ai:
        prompt: "检查移动端 Token 管理，验证使用 SecureStore/Keychain 存储 Token，确认 API 客户端自动处理 Token 刷新"
        context_files: ["apps/mobile/src/utils/auth.ts", "apps/mobile/src/api/client.ts"]
        model: "claude-sonnet-4-6"
