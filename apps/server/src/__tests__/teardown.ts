// Test teardown file - runs after all tests complete
export default async function teardown(): Promise<void> {
  // Clean up any remaining test data
  // Database disconnection will be handled here if needed
}
