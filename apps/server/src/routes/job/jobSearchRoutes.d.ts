/**
 * Job Search Routes
 *
 * REST endpoints for job/candidate search, match results, and search history.
 */
import { Router } from 'express';
declare const router: Router;
export default router;
//# sourceMappingURL=jobSearchRoutes.d.ts.map