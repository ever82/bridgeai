/**
 * BridgeAI Design Tokens
 * Central design token definitions for the BridgeAI design system.
 * Tokens cover colors, typography, spacing, border radius, shadows, and borders.
 */

export { lightTheme, darkTheme, tokens } from './index';

/**
 * Color tokens by category:
 * - Primary: primary, primaryDark, primaryLight
 * - Secondary: secondary, secondaryDark, secondaryLight
 * - Semantic: success, warning, error, info
 * - Neutral: background, backgroundSecondary, backgroundTertiary
 * - Text: text, textSecondary, textTertiary, textInverse
 * - Border: border, borderLight
 * - Overlay: overlay, overlayLight
 */

/**
 * Typography tokens:
 * - Sizes: xs(12), sm(14), base(16), md(18), lg(20), xl(24), 2xl(30), 3xl(36), 4xl(48)
 * - Weights: normal(400), medium(500), semibold(600), bold(700)
 * - Line heights: tight(1.2), normal(1.5), relaxed(1.75)
 * - Letter spacing: tight(-0.5), normal(0), wide(0.5)
 */

/**
 * Spacing tokens:
 * 0, xs(4), sm(8), md(12), base(16), lg(20), xl(24), 2xl(32), 3xl(40), 4xl(48), 5xl(64)
 */

/**
 * Border radius tokens:
 * none(0), sm(4), md(8), lg(12), xl(16), 2xl(20), full(9999)
 */

/**
 * Shadow tokens:
 * none, sm, md, lg, xl
 * Each includes shadowColor, shadowOffset, shadowOpacity, shadowRadius, and elevation
 */
