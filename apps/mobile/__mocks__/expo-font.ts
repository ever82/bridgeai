export const loadAsync = jest.fn(async () => {});
export const isLoaded = jest.fn(() => true);
export const unloadAsync = jest.fn(async () => {});
export default {
  loadAsync,
  isLoaded,
  unloadAsync,
};
